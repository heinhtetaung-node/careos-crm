import {
  stringify
} from "./chunk-GL7CKJX2.js";

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/serialize/bel-serializer.js
var hasOwnProp = Object.prototype.hasOwnProperty;
var MAX_ATTRIBUTES = 64;
function nullable(val, fn, comma) {
  return val || val === 0 || val === "" ? fn(val) + (comma ? "," : "") : "!";
}
function numeric(n, noDefault) {
  if (noDefault) {
    return Math.floor(n).toString(36);
  }
  return n === void 0 || n === 0 ? "" : Math.floor(n).toString(36);
}
function getAddStringContext(obfuscator) {
  let stringTableIdx = 0;
  const stringTable = Object.prototype.hasOwnProperty.call(Object, "create") ? /* @__PURE__ */ Object.create(null) : {};
  return addString;
  function addString(str) {
    if (typeof str === "undefined" || str === "") return "";
    str = obfuscator.obfuscateString(String(str));
    if (hasOwnProp.call(stringTable, str)) {
      return numeric(stringTable[str], true);
    } else {
      stringTable[str] = stringTableIdx++;
      return quoteString(str);
    }
  }
}
function addCustomAttributes(attrs, addString) {
  var attrParts = [];
  Object.entries(attrs || {}).forEach(([key, val]) => {
    if (attrParts.length >= MAX_ATTRIBUTES) return;
    var type = 5;
    var serializedValue;
    key = addString(key);
    switch (typeof val) {
      case "object":
        if (val) {
          serializedValue = addString(stringify(val));
        } else {
          type = 9;
        }
        break;
      case "number":
        type = 6;
        serializedValue = val % 1 ? val : val + ".";
        break;
      case "boolean":
        type = val ? 7 : 8;
        break;
      case "undefined":
        type = 9;
        break;
      default:
        serializedValue = addString(val);
    }
    attrParts.push([type, key + (serializedValue ? "," + serializedValue : "")]);
  });
  return attrParts;
}
var escapable = /([,\\;])/g;
function quoteString(str) {
  return "'" + str.replace(escapable, "\\$1");
}

export {
  nullable,
  numeric,
  getAddStringContext,
  addCustomAttributes
};
//# sourceMappingURL=chunk-54IGZ435.js.map
