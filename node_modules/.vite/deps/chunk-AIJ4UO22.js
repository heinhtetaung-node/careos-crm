// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/timer/timer.js
var Timer = class {
  constructor(opts, ms) {
    if (!opts.onEnd) throw new Error("onEnd handler is required");
    if (!ms) throw new Error("ms duration is required");
    this.onEnd = opts.onEnd;
    this.initialMs = ms;
    this.startTimestamp = Date.now();
    this.timer = this.create(this.onEnd, ms);
  }
  create(cb, ms) {
    if (this.timer) this.clear();
    return setTimeout(() => cb ? cb() : this.onEnd(), ms || this.initialMs);
  }
  clear() {
    clearTimeout(this.timer);
    this.timer = null;
  }
  end() {
    this.clear();
    this.onEnd();
  }
  isValid() {
    return this.initialMs - (Date.now() - this.startTimestamp) > 0;
  }
};

export {
  Timer
};
//# sourceMappingURL=chunk-AIJ4UO22.js.map
