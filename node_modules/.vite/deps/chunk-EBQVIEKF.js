import {
  canEnableSessionTracking
} from "./chunk-YKAZEFH7.js";
import {
  gosNREUMOriginals
} from "./chunk-GL7CKJX2.js";

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/features/session_replay/shared/utils.js
function hasReplayPrerequisite(agentInit) {
  return !!gosNREUMOriginals().o.MO && // Session Replay cannot work without Mutation Observer
  canEnableSessionTracking(agentInit) && // requires session tracking to be running (hence "session" replay...)
  (agentInit == null ? void 0 : agentInit.session_trace.enabled) === true;
}
function isPreloadAllowed(agentInit) {
  return (agentInit == null ? void 0 : agentInit.session_replay.preload) === true && hasReplayPrerequisite(agentInit);
}
function customMasker(text, element) {
  var _a, _b;
  try {
    if (typeof (element == null ? void 0 : element.type) === "string") {
      if (element.type.toLowerCase() === "password") return "*".repeat((text == null ? void 0 : text.length) || 0);
      if (((_a = element == null ? void 0 : element.dataset) == null ? void 0 : _a.nrUnmask) !== void 0 || ((_b = element == null ? void 0 : element.classList) == null ? void 0 : _b.contains("nr-unmask"))) return text;
    }
  } catch (err) {
  }
  return typeof text === "string" ? text.replace(/[\S]/g, "*") : "*".repeat((text == null ? void 0 : text.length) || 0);
}

export {
  hasReplayPrerequisite,
  isPreloadAllowed,
  customMasker
};
//# sourceMappingURL=chunk-EBQVIEKF.js.map
