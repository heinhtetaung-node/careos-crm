// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/timing/now.js
function now() {
  return Math.floor(performance.now());
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/constants/runtime.js
var isBrowserScope = typeof window !== "undefined" && !!window.document;
var isWorkerScope = typeof WorkerGlobalScope !== "undefined" && (typeof self !== "undefined" && self instanceof WorkerGlobalScope && self.navigator instanceof WorkerNavigator || typeof globalThis !== "undefined" && globalThis instanceof WorkerGlobalScope && globalThis.navigator instanceof WorkerNavigator);
var globalScope = isBrowserScope ? window : typeof WorkerGlobalScope !== "undefined" && (typeof self !== "undefined" && self instanceof WorkerGlobalScope && self || typeof globalThis !== "undefined" && globalThis instanceof WorkerGlobalScope && globalThis);
var _a;
var loadedAsDeferredBrowserScript = ((_a = globalScope == null ? void 0 : globalScope.document) == null ? void 0 : _a.readyState) === "complete";
var _a2;
var initiallyHidden = Boolean(((_a2 = globalScope == null ? void 0 : globalScope.document) == null ? void 0 : _a2.visibilityState) === "hidden");
var initialLocation = "" + (globalScope == null ? void 0 : globalScope.location);
var _a3;
var isiOS = /iPad|iPhone|iPod/.test((_a3 = globalScope.navigator) == null ? void 0 : _a3.userAgent);
var iOSBelow16 = isiOS && typeof SharedWorker === "undefined";
var ffVersion = (() => {
  var _a4, _b;
  const match = (_b = (_a4 = globalScope.navigator) == null ? void 0 : _a4.userAgent) == null ? void 0 : _b.match(/Firefox[/\s](\d+\.\d+)/);
  if (Array.isArray(match) && match.length >= 2) {
    return +match[1];
  }
  return 0;
})();
var originTime = Date.now() - now();
var supportsNavTimingL2 = () => {
  var _a4, _b, _c;
  return typeof PerformanceNavigationTiming !== "undefined" && ((_c = (_b = (_a4 = globalScope == null ? void 0 : globalScope.performance) == null ? void 0 : _a4.getEntriesByType("navigation")) == null ? void 0 : _b[0]) == null ? void 0 : _c.responseStart);
};

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/dispatch/global-event.js
var GLOBAL_EVENT_NAMESPACE = "newrelic";
function dispatchGlobalEvent(detail = {}) {
  try {
    globalScope.dispatchEvent(new CustomEvent(GLOBAL_EVENT_NAMESPACE, {
      detail
    }));
  } catch (err) {
  }
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/util/console.js
function warn(code, secondary) {
  if (typeof console.debug !== "function") return;
  console.debug("New Relic Warning: https://github.com/newrelic/newrelic-browser-agent/blob/main/docs/warning-codes.md#".concat(code), secondary);
  dispatchGlobalEvent({
    agentIdentifier: null,
    drained: null,
    type: "data",
    name: "warn",
    feature: "warn",
    data: {
      code,
      secondary
    }
  });
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/util/monkey-patched.js
var checked = /* @__PURE__ */ new Map();
function isNative(...fns) {
  return fns.every((fn) => {
    if (checked.has(fn)) return checked.get(fn);
    const fnString = typeof fn === "function" ? fn.toString() : "";
    const isNative2 = fnString.includes("[native code]");
    const isNr = fnString.includes("nrWrapper");
    if (!isNative2 && !isNr) {
      warn(64, (fn == null ? void 0 : fn.name) || fnString);
    }
    checked.set(fn, isNative2);
    return isNative2;
  });
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/window/nreum.js
var defaults = {
  beacon: "bam.nr-data.net",
  errorBeacon: "bam.nr-data.net"
};
function gosNREUM() {
  if (!globalScope.NREUM) {
    globalScope.NREUM = {};
  }
  if (typeof globalScope.newrelic === "undefined") globalScope.newrelic = globalScope.NREUM;
  return globalScope.NREUM;
}
function gosNREUMInfo() {
  let nr2 = gosNREUM();
  const externallySupplied = nr2.info || {};
  nr2.info = {
    beacon: defaults.beacon,
    errorBeacon: defaults.errorBeacon,
    ...externallySupplied
  };
  return nr2;
}
function gosNREUMLoaderConfig() {
  let nr2 = gosNREUM();
  const externallySupplied = nr2.loader_config || {};
  nr2.loader_config = {
    ...externallySupplied
  };
  return nr2;
}
function gosNREUMInit() {
  let nr2 = gosNREUM();
  const externallySupplied = nr2.init || {};
  nr2.init = {
    ...externallySupplied
  };
  return nr2;
}
function gosNREUMOriginals() {
  let nr2 = gosNREUM();
  if (!nr2.o) {
    nr2.o = {
      ST: globalScope.setTimeout,
      SI: globalScope.setImmediate || globalScope.setInterval,
      CT: globalScope.clearTimeout,
      XHR: globalScope.XMLHttpRequest,
      REQ: globalScope.Request,
      EV: globalScope.Event,
      PR: globalScope.Promise,
      MO: globalScope.MutationObserver,
      // this'll be undefined if not in a web window
      FETCH: globalScope.fetch,
      WS: globalScope.WebSocket
    };
    isNative(...Object.values(nr2.o));
  }
  return nr2;
}
function setNREUMInitializedAgent(id, newAgentInstance) {
  let nr2 = gosNREUM();
  nr2.initializedAgents ?? (nr2.initializedAgents = {});
  newAgentInstance.initializedAt = {
    ms: now(),
    date: /* @__PURE__ */ new Date()
  };
  nr2.initializedAgents[id] = newAgentInstance;
}
function addToNREUM(fnName, fn) {
  let nr2 = gosNREUM();
  nr2[fnName] = fn;
}
function gosCDN() {
  gosNREUMInfo();
  gosNREUMInit();
  gosNREUMOriginals();
  gosNREUMLoaderConfig();
  return gosNREUM();
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/ids/unique-id.js
var uuidv4Template = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx";
function getRandomValue(valueTable, tableIndex) {
  if (valueTable) {
    return valueTable[tableIndex] & 15;
  } else {
    return Math.random() * 16 | 0;
  }
}
function generateUuid() {
  var _a4, _b;
  const crypto = ((_a4 = globalScope) == null ? void 0 : _a4.crypto) || ((_b = globalScope) == null ? void 0 : _b.msCrypto);
  let randomValueTable;
  let randomValueIndex = 0;
  if (crypto && crypto.getRandomValues) {
    randomValueTable = crypto.getRandomValues(new Uint8Array(30));
  }
  return uuidv4Template.split("").map((templateInput) => {
    if (templateInput === "x") {
      return getRandomValue(randomValueTable, randomValueIndex++).toString(16);
    } else if (templateInput === "y") {
      return (getRandomValue() & 3 | 8).toString(16);
    } else {
      return templateInput;
    }
  }).join("");
}
function generateRandomHexString(length) {
  var _a4, _b;
  const crypto = ((_a4 = globalScope) == null ? void 0 : _a4.crypto) || ((_b = globalScope) == null ? void 0 : _b.msCrypto);
  let randomValueTable;
  let randomValueIndex = 0;
  if (crypto && crypto.getRandomValues) {
    randomValueTable = crypto.getRandomValues(new Uint8Array(length));
  }
  const chars = [];
  for (var i = 0; i < length; i++) {
    chars.push(getRandomValue(randomValueTable, randomValueIndex++).toString(16));
  }
  return chars.join("");
}
function generateSpanId() {
  return generateRandomHexString(16);
}
function generateTraceId() {
  return generateRandomHexString(32);
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/ids/bundle-id.js
var bundleId = generateUuid();

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/util/get-or-set.js
var has = Object.prototype.hasOwnProperty;
function getOrSet(obj, prop, getVal) {
  if (has.call(obj, prop)) return obj[prop];
  var val = getVal();
  if (Object.defineProperty && Object.keys) {
    try {
      Object.defineProperty(obj, prop, {
        value: val,
        // old IE inherits non-write-ability
        writable: true,
        enumerable: false
      });
      return val;
    } catch (e) {
    }
  }
  obj[prop] = val;
  return val;
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/event-emitter/event-context.js
var EventContext = class {
  constructor(contextId2) {
    this.contextId = contextId2;
  }
};

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/event-emitter/contextual-ee.js
var contextId = "nr@context:".concat(bundleId);
var globalInstance = ee(void 0, "globalEE");
var nr = gosNREUM();
if (!nr.ee) {
  nr.ee = globalInstance;
}
function ee(old, debugId) {
  var _a4, _b;
  var handlers2 = {};
  var bufferGroupMap = {};
  var emitters = {};
  var isolatedBacklog = false;
  try {
    isolatedBacklog = debugId.length !== 16 ? false : (_b = (_a4 = nr.initializedAgents) == null ? void 0 : _a4[debugId]) == null ? void 0 : _b.runtime.isolatedBacklog;
  } catch (err) {
  }
  var emitter = {
    on: addEventListener,
    addEventListener,
    removeEventListener,
    emit,
    get: getOrCreate,
    listeners,
    context,
    buffer: bufferEventsByGroup,
    abort,
    isBuffering,
    debugId,
    backlog: isolatedBacklog ? {} : old && typeof old.backlog === "object" ? old.backlog : {},
    isolatedBacklog
  };
  function abort() {
    emitter._aborted = true;
    Object.keys(emitter.backlog).forEach((key) => {
      delete emitter.backlog[key];
    });
  }
  Object.defineProperty(emitter, "aborted", {
    get: () => {
      let aborted = emitter._aborted || false;
      if (aborted) return aborted;
      else if (old) {
        aborted = old.aborted;
      }
      return aborted;
    }
  });
  return emitter;
  function context(contextOrStore) {
    if (contextOrStore && contextOrStore instanceof EventContext) {
      return contextOrStore;
    } else if (contextOrStore) {
      return getOrSet(contextOrStore, contextId, () => new EventContext(contextId));
    } else {
      return new EventContext(contextId);
    }
  }
  function emit(type, args, contextOrStore, force, bubble) {
    if (bubble !== false) bubble = true;
    if (globalInstance.aborted && !force) {
      return;
    }
    if (old && bubble) old.emit(type, args, contextOrStore);
    var ctx = context(contextOrStore);
    var handlersArray = listeners(type);
    handlersArray.forEach((handler) => {
      handler.apply(ctx, args);
    });
    var bufferGroup = getBuffer()[bufferGroupMap[type]];
    if (bufferGroup) {
      bufferGroup.push([emitter, type, args, ctx]);
    }
    return ctx;
  }
  function addEventListener(type, fn) {
    handlers2[type] = listeners(type).concat(fn);
  }
  function removeEventListener(type, fn) {
    var listeners2 = handlers2[type];
    if (!listeners2) return;
    for (var i = 0; i < listeners2.length; i++) {
      if (listeners2[i] === fn) {
        listeners2.splice(i, 1);
      }
    }
  }
  function listeners(type) {
    return handlers2[type] || [];
  }
  function getOrCreate(name) {
    return emitters[name] = emitters[name] || ee(emitter, name);
  }
  function bufferEventsByGroup(types, group) {
    const eventBuffer = getBuffer();
    group = group || "feature";
    if (emitter.aborted) return;
    Object.entries(types || {}).forEach(([_, type]) => {
      bufferGroupMap[type] = group;
      if (!(group in eventBuffer)) {
        eventBuffer[group] = [];
      }
    });
  }
  function isBuffering(type) {
    var bufferGroup = getBuffer()[bufferGroupMap[type]];
    return !!bufferGroup;
  }
  function getBuffer() {
    return emitter.backlog;
  }
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/event-emitter/handle.js
var handleEE = globalInstance.get("handle");
function handle(type, args, ctx, group, ee2) {
  if (ee2) {
    ee2.buffer([type], group);
    ee2.emit(type, args, ctx);
  } else {
    handleEE.buffer([type], group);
    handleEE.emit(type, args, ctx);
  }
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/event-emitter/register-handler.js
defaultRegister.on = registerWithSpecificEmitter;
var handlers = defaultRegister.handlers = {};
function defaultRegister(type, handler, group, ee2) {
  registerWithSpecificEmitter(ee2 || handleEE, handlers, type, handler, group);
}
function registerWithSpecificEmitter(ee2, handlers2, type, handler, group) {
  if (!group) group = "feature";
  if (!ee2) ee2 = handleEE;
  var groupHandlers = handlers2[group] = handlers2[group] || {};
  var list = groupHandlers[type] = groupHandlers[type] || [];
  list.push([ee2, handler]);
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/loaders/features/features.js
var EVENTS = "events";
var JSERRORS = "jserrors";
var BLOBS = "browser/blobs";
var RUM = "rum";
var LOGS = "browser/logs";
var FEATURE_NAMES = {
  ajax: "ajax",
  genericEvents: "generic_events",
  jserrors: JSERRORS,
  logging: "logging",
  metrics: "metrics",
  /**
   * @deprecated This feature has been replaced by Generic Events. Use/Import `GenericEvents` instead. This wrapper will be removed in a future release
   */
  pageAction: "page_action",
  pageViewEvent: "page_view_event",
  pageViewTiming: "page_view_timing",
  sessionReplay: "session_replay",
  sessionTrace: "session_trace",
  softNav: "soft_navigations",
  spa: "spa"
};
var featurePriority = {
  [FEATURE_NAMES.pageViewEvent]: 1,
  [FEATURE_NAMES.pageViewTiming]: 2,
  [FEATURE_NAMES.metrics]: 3,
  [FEATURE_NAMES.jserrors]: 4,
  [FEATURE_NAMES.spa]: 5,
  [FEATURE_NAMES.ajax]: 6,
  [FEATURE_NAMES.sessionTrace]: 7,
  [FEATURE_NAMES.softNav]: 8,
  [FEATURE_NAMES.sessionReplay]: 9,
  [FEATURE_NAMES.logging]: 10,
  [FEATURE_NAMES.genericEvents]: 11
};
var FEATURE_TO_ENDPOINT = {
  [FEATURE_NAMES.pageViewEvent]: RUM,
  [FEATURE_NAMES.pageViewTiming]: EVENTS,
  [FEATURE_NAMES.ajax]: EVENTS,
  [FEATURE_NAMES.spa]: EVENTS,
  [FEATURE_NAMES.softNav]: EVENTS,
  [FEATURE_NAMES.metrics]: JSERRORS,
  [FEATURE_NAMES.jserrors]: JSERRORS,
  [FEATURE_NAMES.sessionTrace]: BLOBS,
  [FEATURE_NAMES.sessionReplay]: BLOBS,
  [FEATURE_NAMES.logging]: LOGS,
  [FEATURE_NAMES.genericEvents]: "ins"
};

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/util/stringify.js
var getCircularReplacer = () => {
  const seen = /* @__PURE__ */ new WeakSet();
  return (key, value) => {
    if (typeof value === "object" && value !== null) {
      if (seen.has(value)) {
        return;
      }
      seen.add(value);
    }
    return value;
  };
};
function stringify(val) {
  try {
    return JSON.stringify(val, getCircularReplacer()) ?? "";
  } catch (e) {
    try {
      globalInstance.emit("internal-error", [e]);
    } catch (err) {
    }
    return "";
  }
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/features/metrics/constants.js
var FEATURE_NAME = FEATURE_NAMES.metrics;
var SUPPORTABILITY_METRIC = "sm";
var CUSTOM_METRIC = "cm";
var SUPPORTABILITY_METRIC_CHANNEL = "storeSupportabilityMetrics";
var CUSTOM_METRIC_CHANNEL = "storeEventMetrics";

export {
  now,
  isBrowserScope,
  isWorkerScope,
  globalScope,
  loadedAsDeferredBrowserScript,
  initiallyHidden,
  initialLocation,
  isiOS,
  iOSBelow16,
  ffVersion,
  originTime,
  supportsNavTimingL2,
  dispatchGlobalEvent,
  warn,
  isNative,
  defaults,
  gosNREUM,
  gosNREUMOriginals,
  setNREUMInitializedAgent,
  addToNREUM,
  gosCDN,
  getOrSet,
  EventContext,
  generateUuid,
  generateRandomHexString,
  generateSpanId,
  generateTraceId,
  bundleId,
  contextId,
  globalInstance,
  handle,
  defaultRegister,
  EVENTS,
  JSERRORS,
  BLOBS,
  RUM,
  LOGS,
  FEATURE_NAMES,
  featurePriority,
  FEATURE_TO_ENDPOINT,
  stringify,
  FEATURE_NAME,
  SUPPORTABILITY_METRIC,
  CUSTOM_METRIC,
  SUPPORTABILITY_METRIC_CHANNEL,
  CUSTOM_METRIC_CHANNEL
};
/*! Bundled license information:

@newrelic/browser-agent/dist/esm/common/constants/runtime.js:
  (**
   * @file Contains constants about the environment the agent is running
   * within. These values are derived at the time the agent is first loaded.
   * @copyright 2023 New Relic Corporation. All rights reserved.
   * @license Apache-2.0
   *)

@newrelic/browser-agent/dist/esm/common/ids/bundle-id.js:
  (**
   * @file Contains a unique identifier for the running agent bundle
   * when loaded.
   * @copyright 2023 New Relic Corporation. All rights reserved.
   * @license Apache-2.0
   *)
*/
//# sourceMappingURL=chunk-GL7CKJX2.js.map
