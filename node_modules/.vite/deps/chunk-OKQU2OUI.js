// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/util/invoke.js
function debounce(func, timeout = 500, options = {}) {
  const leading = (options == null ? void 0 : options.leading) || false;
  let timer;
  return (...args) => {
    if (leading && timer === void 0) {
      func.apply(this, args);
      timer = setTimeout(() => {
        timer = clearTimeout(timer);
      }, timeout);
    }
    if (!leading) {
      clearTimeout(timer);
      timer = setTimeout(() => {
        func.apply(this, args);
      }, timeout);
    }
  };
}
function single(func) {
  let called = false;
  return (...args) => {
    if (!called) {
      called = true;
      func.apply(this, args);
    }
  };
}

export {
  debounce,
  single
};
//# sourceMappingURL=chunk-OKQU2OUI.js.map
