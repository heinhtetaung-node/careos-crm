import {
  globalScope
} from "./chunk-GL7CKJX2.js";

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/url/parse-url.js
function parseUrl(url) {
  var _a, _b, _c, _d;
  if ((url || "").indexOf("data:") === 0) {
    return {
      protocol: "data"
    };
  }
  try {
    const parsedUrl = new URL(url, location.href);
    const returnVal = {
      port: parsedUrl.port,
      hostname: parsedUrl.hostname,
      pathname: parsedUrl.pathname,
      search: parsedUrl.search,
      protocol: parsedUrl.protocol.slice(0, parsedUrl.protocol.indexOf(":")),
      sameOrigin: parsedUrl.protocol === ((_b = (_a = globalScope) == null ? void 0 : _a.location) == null ? void 0 : _b.protocol) && parsedUrl.host === ((_d = (_c = globalScope) == null ? void 0 : _c.location) == null ? void 0 : _d.host)
    };
    if (!returnVal.port || returnVal.port === "") {
      if (parsedUrl.protocol === "http:") returnVal.port = "80";
      if (parsedUrl.protocol === "https:") returnVal.port = "443";
    }
    if (!returnVal.pathname || returnVal.pathname === "") {
      returnVal.pathname = "/";
    } else if (!returnVal.pathname.startsWith("/")) {
      returnVal.pathname = "/".concat(returnVal.pathname);
    }
    return returnVal;
  } catch (err) {
    return {};
  }
}

export {
  parseUrl
};
//# sourceMappingURL=chunk-PM7QNTT2.js.map
