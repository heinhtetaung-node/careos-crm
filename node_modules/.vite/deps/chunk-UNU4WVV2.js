import {
  single
} from "./chunk-OKQU2OUI.js";
import {
  documentAddEventListener,
  windowAddEventListener
} from "./chunk-7NGBQRL6.js";

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/window/load.js
function checkState() {
  return typeof document === "undefined" || document.readyState === "complete";
}
function onWindowLoad(cb, useCapture) {
  if (checkState()) return cb();
  const singleCb = single(cb);
  const poll = setInterval(() => {
    if (checkState()) {
      clearInterval(poll);
      singleCb();
    }
  }, 500);
  windowAddEventListener("load", singleCb, useCapture);
}
function onDOMContentLoaded(cb) {
  if (checkState()) return cb();
  documentAddEventListener("DOMContentLoaded", cb);
}
function onPopstateChange(cb) {
  if (checkState()) return cb();
  windowAddEventListener("popstate", cb);
}

export {
  onWindowLoad,
  onDOMContentLoaded,
  onPopstateChange
};
//# sourceMappingURL=chunk-UNU4WVV2.js.map
