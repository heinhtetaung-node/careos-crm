import {
  documentAddEventListener,
  windowAddEventListener
} from "./chunk-7NGBQRL6.js";
import {
  defaults,
  warn
} from "./chunk-GL7CKJX2.js";

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/window/page-visibility.js
function subscribeToVisibilityChange(cb, toHiddenOnly = false, capture, abortSignal) {
  documentAddEventListener("visibilitychange", handleVisibilityChange, capture, abortSignal);
  function handleVisibilityChange() {
    if (toHiddenOnly) {
      if (document.visibilityState === "hidden") cb();
      return;
    }
    cb(document.visibilityState);
  }
}
function subscribeToPageUnload(cb, capture, abortSignal) {
  windowAddEventListener("pagehide", cb, capture, abortSignal);
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/config/configurable.js
function getModeledObject(obj, model) {
  try {
    if (!obj || typeof obj !== "object") return warn(3);
    if (!model || typeof model !== "object") return warn(4);
    const output = Object.create(Object.getPrototypeOf(model), Object.getOwnPropertyDescriptors(model));
    const target = Object.keys(output).length === 0 ? obj : output;
    for (let key in target) {
      if (obj[key] === void 0) continue;
      try {
        if (obj[key] === null) {
          output[key] = null;
          continue;
        }
        if (Array.isArray(obj[key]) && Array.isArray(model[key])) output[key] = Array.from(/* @__PURE__ */ new Set([...obj[key], ...model[key]]));
        else if (typeof obj[key] === "object" && typeof model[key] === "object") output[key] = getModeledObject(obj[key], model[key]);
        else output[key] = obj[key];
      } catch (e) {
        if (!output[key]) warn(1, e);
      }
    }
    return output;
  } catch (err) {
    warn(2, err);
  }
}

// node_modules/.pnpm/@newrelic+browser-agent@1.306.0/node_modules/@newrelic/browser-agent/dist/esm/common/config/info.js
var InfoModel = {
  // preset defaults
  beacon: defaults.beacon,
  errorBeacon: defaults.errorBeacon,
  // others must be populated by user
  licenseKey: void 0,
  applicationID: void 0,
  sa: void 0,
  queueTime: void 0,
  applicationTime: void 0,
  ttGuid: void 0,
  user: void 0,
  account: void 0,
  product: void 0,
  extra: void 0,
  jsAttributes: {},
  userAttributes: void 0,
  atts: void 0,
  transactionName: void 0,
  tNamePlain: void 0
};
function isValid(info) {
  try {
    return !!info.licenseKey && !!info.errorBeacon && !!info.applicationID;
  } catch (err) {
    return false;
  }
}
var mergeInfo = (info) => {
  return getModeledObject(info, InfoModel);
};

export {
  subscribeToVisibilityChange,
  subscribeToPageUnload,
  getModeledObject,
  isValid,
  mergeInfo
};
//# sourceMappingURL=chunk-XIYH7BID.js.map
