import { DataSchema, IntrisicConstructorConfig } from './interface';

import InputContainer from '../InputContainer';

export const defaultConstructorConfig: IntrisicConstructorConfig = {
  title: 'Title',
  Row: InputContainer,
};

export const defaultDataSchema: DataSchema = {};
